% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cache.R
\name{.bch_fetch_resource}
\alias{.bch_fetch_resource}
\title{Fetch a validated BCH JSON resource}
\usage{
.bch_fetch_resource(
  resource = c("catalog", "series"),
  id = NULL,
  source = "auto",
  refresh = FALSE,
  max_age = NULL,
  cache_dir = NULL
)
}
\arguments{
\item{resource}{Either \code{"catalog"} or \code{"series"}.}

\item{id}{One series identifier; NULL for the catalog.}

\item{source}{Either \code{"auto"}, \code{"live"}, or \code{"cache"}.}

\item{refresh}{Force live retrieval without fallback.}

\item{max_age}{Cache freshness in seconds. Defaults to 86400 for the catalog
and 21600 for a series. \code{source = "cache"} permits stale entries and marks
them explicitly.}

\item{cache_dir}{Cache directory, NULL for the \code{bch.cache_dir} option or
\code{tools::R_user_dir("bchR", "cache")}, or FALSE to disable caching.}
}
\value{
A list containing parsed \code{payload}, \code{provenance}, and \code{attempts}.
}
\description{
The cache stores a versioned JSON envelope containing only the public raw
response body and a small, fixed set of provenance fields. Cache hits never
require credentials. \code{auto} uses a fresh cache, otherwise requests the API;
stale fallback is limited to a missing key or a transient network/server
failure. Authentication rejection never falls back. \code{refresh = TRUE}
requires a live response and disables fallback.
}
\keyword{internal}
