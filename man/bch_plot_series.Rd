% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-series.R
\name{bch_plot_series}
\alias{bch_plot_series}
\title{Plot observed indicator series without implicit transformations}
\usage{
bch_plot_series(
  x,
  id = NULL,
  title = NULL,
  subtitle = NULL,
  caption = NULL,
  mode = "level",
  ...
)
}
\arguments{
\item{x}{A long-form \code{bch_result} or data frame with \code{indicator_id}, numeric
\code{value} and Date columns \code{period_start}, \code{reference_date} or \code{date}.}

\item{id}{Optional indicator ID or vector of IDs to select.}

\item{title, subtitle}{Optional plain-text plot labels.}

\item{caption}{Optional text placed before the source and retrieval date.
Attribution is always retained; data frames without provenance are
labelled as user-provided data.}

\item{mode}{Currently only \code{"level"}: display values exactly as supplied,
with no growth-rate calculation, aggregation or interpolation.}

\item{...}{Reserved for future use. Must be empty.}
}
\value{
A \code{ggplot2} plot. The plot's data include explicit segment IDs so
gaps remain visible. No changes are made to the input.
}
\description{
Uses supplied long-form observations only; this function never downloads
data. Annual, quarterly, monthly and daily series are split at absent
periods and missing values. Series with unknown frequency are shown as
points because continuity cannot be established. Different units use
separate panels. Metadata can supply descriptions, frequency and units.
A colour-vision-friendly viridis palette and angled date labels are used
by default. The returned plot can be further customized with \code{ggplot2}.
}
