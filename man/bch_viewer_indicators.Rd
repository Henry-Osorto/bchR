% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bch_viewer_indicators.R
\name{bch_viewer_indicators}
\alias{bch_viewer_indicators}
\title{Browse BCH indicators in an interactive table}
\usage{
bch_viewer_indicators(
  progress = TRUE,
  show_search = TRUE,
  striped = TRUE,
  bordered = FALSE,
  compact = FALSE,
  highlight = TRUE,
  full_width = TRUE,
  page_size = 15,
  open_browser = FALSE
)
}
\arguments{
\item{progress}{Logical. If \code{TRUE} (default), progress messages generated by
\code{\link[=bch_get_indicators]{bch_get_indicators()}} are displayed.}

\item{show_search}{Logical. If \code{TRUE} (default), enables global search over
the displayed catalogue columns.}

\item{striped}{Logical. If \code{TRUE} (default), alternating row shading is used.}

\item{bordered}{Logical. If \code{TRUE}, table borders are displayed. Defaults to
\code{FALSE}.}

\item{compact}{Logical. If \code{TRUE}, reduces table-cell padding. Defaults to
\code{FALSE}.}

\item{highlight}{Logical. If \code{TRUE} (default), rows are highlighted on hover.}

\item{full_width}{Logical. If \code{TRUE} (default), the table uses the available
width.}

\item{page_size}{Positive whole number. Number of rows shown per page.
Defaults to \code{15}.}

\item{open_browser}{Logical. If \code{TRUE}, the generated viewer is saved to a
temporary HTML file and opened in the system browser. Defaults to \code{FALSE}.}
}
\value{
A browsable HTML object containing a BCH-specific header and a
\code{reactable} table. The same object is returned when \code{open_browser = TRUE}.
}
\description{
Creates an interactive HTML viewer for the indicator catalogue of the Banco
Central de Honduras (BCH). The catalogue is obtained exclusively through
\code{\link[=bch_get_indicators]{bch_get_indicators()}}, so this function does not construct API URLs or
manage \code{BCH_API_KEY} directly.
}
\details{
The viewer displays the standard catalogue fields \code{Id}, \code{Nombre},
\code{Descripcion}, \code{Periodicidad}, \code{Grupo}, and \code{CorrelativoGrupo}, using Spanish
interface labels. Columns are sortable and resizable, pagination is enabled,
and users can change the number of rows per page.

\code{Descripcion} is rendered with wrapping so long official descriptions can be
read without truncating their content. The viewer does not modify the
catalogue returned by \code{\link[=bch_get_indicators]{bch_get_indicators()}}.

Logos are optional. If compatible image files have explicitly been bundled
under \verb{inst/images/}, they are embedded locally as data URIs. The function
never downloads external images at runtime and does not require logos to be
present.

The current implementation intentionally retains \code{reactable}, consistent
with the viewer design used in \code{CepalStatR}. The BCH catalogue is a
client-side table, so very large catalogues can increase HTML/widget size and
browser memory use. Pagination limits the number of rows rendered visibly at
one time, while global search still operates over the client-side catalogue.
}
\examples{
\dontrun{
bch_viewer_indicators()

bch_viewer_indicators(
  page_size = 25,
  open_browser = TRUE
)

bch_viewer_indicators(
  progress = FALSE,
  show_search = TRUE,
  compact = TRUE
)
}

}
\seealso{
\code{\link[=bch_get_indicators]{bch_get_indicators()}}, \code{\link[=bch_set_api_key]{bch_set_api_key()}}
}
