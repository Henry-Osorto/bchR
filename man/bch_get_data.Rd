% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\name{bch_get_data}
\alias{bch_get_data}
\title{Retrieve BCH observations for selected indicators or groups}
\usage{
bch_get_data(
  id = NULL,
  group = NULL,
  catalog = NULL,
  start = NULL,
  end = NULL,
  shape = c("long", "wide"),
  source = c("auto", "live", "cache"),
  refresh = FALSE,
  on_error = c("stop", "collect"),
  max_series = 25L,
  max_age = NULL,
  unknown_frequency = c("error", "keep"),
  progress = interactive(),
  cache_dir = NULL
)
}
\arguments{
\item{id}{Indicator ID(s), mutually exclusive with \code{group}.}

\item{group}{Exact group code(s), mutually exclusive with \code{id}.}

\item{catalog}{Optional catalog bch_result or data frame.}

\item{start, end}{Optional inclusive Date or ISO YYYY-MM-DD limits.}

\item{shape}{Long observations or wide period-aligned values.}

\item{source}{Resource source: \code{"auto"}, \code{"live"}, or \code{"cache"}.}

\item{refresh}{Whether to refresh resources when permitted by \code{source}.}

\item{on_error}{\code{"stop"} returns no partial result on failure; successful
earlier requests may still have updated the local cache. \code{"collect"}
records per-series failures in \code{problems} and returns available series.}

\item{max_series}{Maximum distinct indicators allowed in one call.}

\item{max_age}{Maximum cache age in seconds; NULL uses resource defaults.}

\item{unknown_frequency}{Error on an unverified frequency or keep raw dates.}

\item{progress}{Whether to report retrieval progress with messages.}

\item{cache_dir}{Optional explicit cache directory.}
}
\value{
A bch_result. Wide value columns have names \verb{id_<ID>} and are
documented in \code{column_map}. Metadata includes all selected series,
including failures; column_map includes successfully parsed series.
}
\description{
Supply exactly one of \code{id} or \code{group}. A supplied catalog is used locally
and never triggers a catalog request. Series are retrieved sequentially.
The series limit is checked before any series request. \code{source = "cache"}
works without credentials and never connects to BCH.
}
\details{
Dates are filtered locally, with inclusive period overlap: a period is
kept if its end is on/after \code{start} and its start is on/before \code{end}.
UTC reference dates and raw timestamps are retained in long output.
Unknown periods cannot be date-filtered or reshaped to wide output.
No aggregation, filling, frequency conversion, or imputation is performed.
}
